2008-10-31  Gunnar Wrobel  <p@rdus.de>

	* modules/service_phpldapadmin/manifests/init.pp: Add phpldapadmin
	as a basic LDAP manager.

	* modules/service_postfix/manifests/init.pp: The horde framework
	has been moved to a separate module.

	* modules/tool_php/manifests/init.pp: Add the "hash" use flag for
	phpldapadmin and reorder the flags.

	* modules/tool_horde_framework/manifests/init.pp: Do not define
	the horde framework installation in separate modules.

2008-10-30  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_openssl/manifests/init.pp: Use the kolab variables
	to indicate the ssl certificate locations.

	* modules/service_sasl/manifests/init.pp: Use the kolab sasl conf
	dir.

	* modules/service_openldap/manifests/init.pp: Use
	openldap-2.4.11. 2.4.7 still seems to have some serious bugs.

	* modules/tool_php/manifests/init.pp: Add PEAR-CodeSniffer to the
	php tools.

2008-10-19  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_emacs/manifests/init.pp: A number of emacs config
	settings with the focus on php editing.

	* modules/service_lighttpd/manifests/init.pp: Fixed version
	handling in the lighttpd module.

	* modules/meta_kolab_www/manifests/init.pp: Add the php toolset.

	* modules/service_apache/manifests/init.pp: Remove php here.

	* modules/service_openldap/manifests/init.pp: Fixed module name.

	* modules/service_postfix/manifests/init.pp: Fix the use flag
	name.

2008-10-15  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_php/manifests/init.pp: Add the correct c-client use
	flags and add php-docs.

	* modules/tool_php/README: Add php installation.

	* modules/tool_system/manifests/init.pp: Add overlay-utils.

2008-10-14  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_emacs/manifests/init.pp: Add geben.

	* modules/tool_system/manifests/init.pp: Add mercurial.

	* modules/tool_portage/manifests/init.pp: Allow ignoring eclass
	overwrites on systems where we work with Gentoo CVS.

	* modules/tool_emacs/manifests/init.pp: Add the emacs use flag to
	some packages.

	* modules/tool_system/manifests/init.pp: Added keywords.

	* modules/tool_emacs/manifests/init.pp: Added keywords.

	* modules/meta_de_pardus_desktop/manifests/init.pp: Add new
	modules.

	* modules/tool_system_desktop/*: Added some desktop tools.

	* modules/tool_gfx/*: Added some desktop gfx tools.

	* modules/tool_baselayout/manifests/init.pp: openrc update.

	* modules/tool_baselayout/manifests/init.pp: Fixed timezone
	setting.

	* modules/service_cron/manifests/init.pp: Allow for a different
	cron mode for systems that are not continuously running.

	* modules/service_ssmtp/manifests/init.pp: Fixed masked ssmtp
	version.

	* modules/tool_emacs/manifests/init.pp: Extended the emacs
	configuration.

2008-10-13  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_system/manifests/init.pp: Always install lsof.

2008-10-10  Gunnar Wrobel  <p@rdus.de>

	* modules/service_cyrusimap/manifests/init.pp: A first working
	version of the cyrus IMAP configuration.

	* modules/tool_baselayout/manifests/init.pp: Add the timezone
	setting.

	* modules/service_kolabd/manifests/init.pp: A draft module for the
	kolabd daemon.

	* modules/kolab_service_kolab/*: Renamed service_kolab module to
	kolab_service_kolab.

2008-10-09  Gunnar Wrobel  <p@rdus.de>

	* modules/service_cyrusimap/*: Continued the cyrus-imapd module
	draft.

	* modules/tool_system/manifests/init.pp: Screen should be on any
	machine.

	* modules/tool_portage/templates/make.conf_2.1.4.4: Correct
	handling of the binhost setting.

	* modules/tool_openssl/manifests/init.pp: Fix the handling of ssl
	certificate paths.

	* modules/tool_ec2_kernel/manifests/init.pp: Fixes to supress
	rerunning the task.

	* modules/tool_bash/*: Provide a decent bash setup for using
	screen sessions.

	* modules/service_openldap/manifests/init.pp: Always use the
	useradd provider, fixes for the ssl certificate path.

	* modules/service_kolab/plugins/facter/kolab.rb: Do not empty the
	facts before reading additional config files.

	* modules/service_kolab/manifests/init.pp: Always use the groupadd
	provider, fixes for the ssl certificate path.

	* modules/service_cron/manifests/init.pp: Always use the groupadd
	provider.

	* modules/service_clamav/manifests/init.pp: Always use the useradd
	provider.

	* modules/tool_ntp/manifests/init.pp: Added ntp module.

	* modules/tool_ec2_kernel/manifests/init.pp: Fix kernel config
	generation.

	* modules/tool_fs/manifests/init.pp: Ensure the loop kernel module
	is loaded. Disable autofs for now.

	* modules/service_cyrusimap/manifests/init.pp: Draft version of
	the imap installation.

	* modules/meta_kolab_imap/manifests/init.pp: Start the definition
	of the Kolab IMAP part.

	* modules/meta_kolab_monitor/manifests/init.pp: Comment the nagios
	part as it is currently outdated.

	* modules/service_openldap/files/kolab2.schema: Allow a
	kolabFreeBusyServer setting in LDAP.

	* modules/service_freebusy/manifests/init.pp: New free/busy
	version.

2008-10-08  Gunnar Wrobel  <p@rdus.de>

	* staging/Rakefile: Fix the rebundle and update task.

	* modules/tool_bash/files/bash-prompt: Fix bash prompt again.

	* staging/Rakefile: Fix typo and include the facts.

	* modules/tool_fs/manifests/init.pp: Add the loop module.

	* modules/tool_pardalys/manifests/init.pp: Fix keyword.

	* staging/defaults.yml: Cleanup default settings.

	* staging/bootstrap/site.pp.erb: Reorganize to indicate the
	optional parts .

	* staging/Rakefile: Additional cleanup, a rebundle function and
	fixes to bundling.

	* modules/tool_baselayout/templates/net_real: Add the net
	template.

	* staging/bootstrap/site.pp.erb: Syntax fixes.

	* staging/Rakefile: Add a reinit task.

	* modules/tool_portage/*: Add binhost_only option.

	* modules/tool_pardalys/manifests/init.pp: Use unstable facter.

	* modules/tool_ec2_kernel/manifests/init.pp: Add kernel module
	loading.

	* modules/meta_de_pardus_ec2/manifests/init.pp: Add new module for
	net filesystem support.

	* modules/tool_baselayout/manifests/init.pp: Add support for the
	net configuration.
	Activate numlock only on physical machines.

	* modules/tool_system/manifests/init.pp: Add basic dhcpcd package.

	* modules/tool_ec2_kernel/manifests/init.pp: Fix path for the ec2
	kernel modules.

	* modules/tool_ec2_tools/plugins/facter/metadata.rb: Added the
	module to fetch ec2 metadata.

	* staging/Rakefile (chroot): Allow to customize the image name.

	* modules/tool_ec2_kernel/manifests/init.pp: Fix the depmod call.

	* modules/tool_baselayout/templates/fstab: Added fstab template.

	* staging/bootstrap/site.pp.erb: Add fstab information.

	* staging/bootstrap/make.conf.erb: Remove the exclude option.

	* staging/Rakefile: Only mount portage if we are bootstrapping on
	Gentoo, do not link /tmp into /var/tmp and slightly modify
	bundling.

	* modules/tool_ec2_kernel/manifests/init.pp: Require unifdef.

	* modules/tool_bash/files/bash-prompt: Fixed the bash prompt.

	* modules/tool_baselayout/manifests/init.pp: Fix the dependency
	handling between openrc and baselayout, allow overwriting
	/etc/fstab.

	* modules/tool_ec2_kernel/*: Adapt the kernel configuration to the
	hardwaremodel used.

2008-10-07  Gunnar Wrobel  <p@rdus.de>

	* staging/Rakefile (chroot): Make removal of the script file
	optional.

	* modules/tool_ec2_kernel/manifests/init.pp: Fix the name of the
	module zip file.

	* modules/tool_ec2_tools/manifests/init.pp: xml-simple is also
	required for aws-s3.

	* modules/tool_ec2_kernel/manifests/init.pp: We require the kernel
	package to be installed first.

	* staging/Rakefile: Removed the configure step.

	* staging/bootstrap/site.pp.erb: Add the hardwaremodel parameter.

	* modules/tool_ec2_tools/manifests/init.pp: Add some ec2 bootup
	helper scripts.
	Add a line to local.start to init the instance.

	* modules/tool_ec2_tools/files/*: Add some ec2 bootup helper
	scripts.

	* modules/tool_ec2_tools/manifests/init.pp: Unmask additional
	dependencies.

	* modules/meta_de_pardus_ec2/manifests/init.pp: Add the ec2 tools
	module.

	* modules/tool_ec2_tools/manifests/init.pp: Some ec2 tools wrapped
	in a module.

	* modules/tool_baselayout/manifests/init.pp: Update to
	openrc-0.3.0.

	* modules/meta_de_pardus_ec2/manifests/init.pp: Started the ec2
	profile.

	* modules/tool_ec2_kernel/*: Add a new module for the ec2 kernel.

	* staging/Rakefile: Complete the basic image setup.

	* modules/service_cron/manifests/init.pp: Ensure some additional
	prerequisites are met.

	* modules/tool_portage/manifests/init.pp: Ensure
	/etc/portage/make.conf.puppet exists.

	* staging/bootstrap/site.pp.erb: Fixed typo.

	* staging/defaults.yml: Removed excludes, added admin info.

	* staging/bootstrap/site.pp.erb: Completed the pardalys bootstrap
	definition.

	* modules/meta_de_pardus_common/manifests/init.pp: Adapt to the
	cleaned modules.

	* modules/tool_emacs/manifests/init.pp: Complete documentation,
	refresh site dir after 72git-gentoo.el update, install basic
	.emacs file into /etc/skel.

	* modules/service_syslog/manifests/init.pp: Complete documentation
	and minor cleanups.

	* modules/service_openssh/manifests/init.pp: Complete
	documentation and minor cleanups.

	* bin/pardalys: puppet-0.24.5 seems to ignore the clientbucketdir
	setting from puppet.conf. Set it in the pardalys script.

	* modules/tool_baselayout/*: Complete documentation, fixed
	variable names, allowed overriding the build type for images, some
	cleanup.

	* modules/tool_portage/manifests/init.pp: Complete documentation,
	variable cleanup and removed rsync excludes.

	* modules/tool_portage/templates/make.conf_2.1.4.4: Always set
	ACCEPT_KEYWORDS, remove PORTAGE_RSYNC_EXTRA_OPTS, activate mail
	and summary logs, cleaned variable names.

	* modules/tool_portage/templates/glsa-check: Remove the nagios
	notification.

	* modules/tool_subversion/manifests/init.pp: Complete
	documentation.

	* modules/service_ssmtp/*: Complete documentation and minor
	cleanups.

	* modules/service_monit/*: Complete documentation and minor
	cleanups.

	* modules/service_cron/*: Complete documentation and minor
	cleanups.

	* modules/client_puppet_local/*: Removed in favor of
	tool_pardalys.

	* modules/tool_system/manifests/init.pp: Complete documentation
	and some cleanup.

	* modules/tool_pardalys/templates/puppet.conf: Allow different
	pardalys bootstrap types.

	* modules/tool_pardalys/manifests/init.pp: Complete documentation,
	allow different pardalys bootstrap types, include settings from
	the old client_puppet_local module.

	* modules/os/manifests/init.pp: Complete documentation.

	* modules/tool_openssl/manifests/init.pp: Complete documentation
	and provide additional paths to the ssl information within the
	module.

	* modules/tool_layman/manifests/init.pp: Complete documentation
	and some cleanup.

	* modules/tool_gpg/*: Complete documentation.

	* modules/tool_bash/*: Complete documentation.

	* modules/tool_logrotate/manifests/init.pp: Complete documentation
	and require the backup directory.

	* modules/tool_backup/manifests/init.pp: Complete documentation
	and require the backup directory.

	* modules/tool_backupdir/*: Added a separate tool_backupdir module
	as prerequisite for tool_backup and tool_logrotate.

2008-10-05  Gunnar Wrobel  <p@rdus.de>

	* staging/*: Added a staging area derived from
	http://github.com/dysinger/gec2/tree/master for building images
	based on pardalys. This is just an early draft.

2008-10-01  Gunnar Wrobel  <p@rdus.de>

	* modules/service_freebusy/manifests/init.pp: Update to the latest
	packages.

2008-09-29  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_emacs/manifests/init.pp: Provide the buildhost with
	editing facilities for pardalys.

	* modules/service_freebusy/manifests/init.pp: Complete the PEAR
	package renaming.

	* modules/service_freebusy/templates/freebusy_config.php_0.0.4.20080925:
	Configuration for the new free/busy version.

	* modules/service_postfix/manifests/init.pp: Adapt to updates of
	Horde PEAR packages.

	* modules/service_freebusy/manifests/init.pp: Adapt to updates of
	Horde PEAR packages.

	* modules/tool_emacs/templates/dot_emacs: The admin credentials
	used here may not be the same as for the sysadmin.

	* modules/tool_emacs/manifests/init.pp: The admin credentials used
	here may not be the same as for the sysadmin.

	* modules/service_spamassassin/manifests/init.pp: Use the correct
	sysadmin variable.

	* modules/service_nagios/manifests/init.pp: Fixed the way we
	determine the nagios version number.

2008-09-19  Gunnar Wrobel  <p@rdus.de>

	* modules/service_horde/manifests/init.pp: Fixed the variable we
	use for specifiying the LDAP host.

	* modules/service_freebusy/manifests/init.pp: Fixed the variable
	we use for specifiying the LDAP host.

	* modules/tool_pardalys/manifests/init.pp: Fixed the way we handle
	the pardalys configuration.

	* etc/package_versions: Replace hyphens with underscores.

	* modules/service_horde/manifests/init.pp: Prepare the hermes
	installation.
	Fix the hermes installation.

	* modules/service_freebusy/manifests/init.pp: Accept
	Horde_Date-0.1.0.

	* modules/service_horde/manifests/init.pp: Accept all
	horde-webmail versions as unstable at the moment.

	* modules/tool_openssl/manifests/init.pp: Ensure the SSL
	certificate directory exists.

	* modules/service_horde/manifests/init.pp: Avoid clash with
	service_freebusy.

	* modules/meta_kolab_smtp/manifests/init.pp: Added the SSL
	configuration.

	* modules/service_freebusy/manifests/init.pp: Avoid clash with
	service_horde.

	* modules/service_postfix/manifests/init.pp: Fixed definition
	clashes with service_freebusy.

	* modules/service_freebusy/manifests/init.pp: It is not necessary
	to require installation of dependencies. Fixed definition clashes
	with service_postfix.

	* modules/service_syslog/*: Started reorganizing the logwatch scripts.

	* modules/service_logwatch/*: Started reorganizing the logwatch scripts.

	* modules/meta_kolab_monitor/manifests/init.pp: Include the SSL
	configuration for the monitoring host.

2008-09-18  Gunnar Wrobel  <p@rdus.de>

	* modules/service_horde/manifests/init.pp: Accept all 1.2 versions
	of horde.

	* modules/service_monit/templates/monitrc_4.10.1: Use the unified
	SSL setup.

	* modules/service_horde/manifests/init.pp: Added the vhost
	configuration and allowed redirection targets.

	* modules/service_horde/templates/horde_vhost.conf: Use the
	unified SSL setup and allow redirection vhosts.

	* modules/service_freebusy/templates/freebusy_vhost.conf: Use the
	unified SSL setup.

	* modules/service_freebusy/manifests/init.pp: Added missing
	required packages.

	* modules/service_apache/files/00_default_ssl_vhost.conf: New SSL
	default host configuration that activates name based SSL vhosting.

	* modules/service_apache/manifests/init.pp: Added a modified
	default SSL host configuration.

	* modules/tool_openssl/manifests/init.pp: Added too_openssl for a
	unified SSL setup.

	* etc/package_versions (Kolab_FreeBusy): Added the Kolab free/busy
	package.

2008-09-16  Gunnar Wrobel  <p@rdus.de>

	* modules/service_apache/templates/apache2: Added -D STATUS for
	apache monitoring.

	* modules/service_horde/manifests/init.pp: Update to
	horde-webmail-1.2_rc1.

	* modules/service_apache/manifests/init.pp: Added apache
	monitoring.

	* modules/service_freebusy/*: Completed free/busy support.

	* modules/service_apache/templates/apache2: Activate the default
	ssl vhost.

2008-09-15  Gunnar Wrobel  <p@rdus.de>

	* modules/service_freebusy/manifests/init.pp: Correct filenames
	for unmasking and add the iCalendar library in the newer version.

	* modules/service_apache/manifests/init.pp: Add some php use
	flags.

	* modules/service_freebusy/*: Added definition for Kolab
	free/busy.

2008-09-09  Gunnar Wrobel  <p@rdus.de>

	* modules/service_monit/templates/monitrc_4.10.1: Alert the system
	administrator.
	Fixed the sender.

	* modules/service_monit/manifests/init.pp: Update and fix the
	monit configuration.

	* modules/service_ssmtp/manifests/init.pp: Corrected location for
	the ssmtp.conf file.

	* modules/service_syslog/manifests/init.pp: Remove the munin
	config for syslog-ng.

	* modules/service_apache/templates/apache2: Allow additional
	apache startup options.

	* modules/tool_portage/manifests/init.pp: Ensure
	/usr/portage/disfiles exists.

	* modules/service_cron/templates/crontab: Add initial crontab.

	* modules/tool_system/manifests/init.pp: Add my scripts package to
	the set of installed packages.
	Add files security check.

	* modules/tool_logrotate/*: Add log rotation.

	* modules/tool_system/manifests/init.pp: No need for the
	/var/backup directories.

	* modules/service_apache/manifests/init.pp: Fix link to startup
	script.

	* modules/tool_system/manifests/init.pp: Allow a higher glibc on
	virtual servers.

2008-09-08  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_system/manifests/init.pp: Add strace and ltrace as
	basic tools.

	* modules/service_ssmtp/manifests/init.pp: Mask a broken ssmtp for
	now.

	* modules/tool_system/manifests/init.pp: Add a telnet client to
	the systems.

	* modules/root/plugins/puppet/parser/functions/split.rb: Add
	documentation.

2008-08-22  Gunnar Wrobel  <p@rdus.de>

	* modules/service_horde/manifests/init.pp: Move to
	horde-webmail-1.1.2-r1.

	* modules/service_openldap/templates/slapd.conf_2.4.7: Correct
	handling CA certificates.

	* modules/service_openldap/manifests/init.pp: Correct handling CA
	certificates.

	* modules/service_openldap/templates/slapd.conf_2.4.7: Ensure we
	can correctly use CACert certificates.

	* modules/service_apache/manifests/init.pp: Add mod_python.

	* modules/tool_subversion/manifests/init.pp: Subversion should
	provide apache support.

	* modules/tool_system/manifests/init.pp: All systems should
	provide slocate and sqlite.

	* modules/service_spamassassin/templates/sa-update: Trying to get
	a stable restart of amavisd.

	* modules/tool_portage/files/esync: Remove eix output.

	* modules/tool_portage/manifests/init.pp: Set the portage default
	version.

2008-08-21  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_backup/*: Added a simple /etc backup routine.

	* modules/service_clamav/templates/freshclam.conf_0.93.3:
	* modules/service_clamav/templates/clamd.conf_0.93.3:
	Update to clamav-0.93.3.

	* modules/service_clamav/templates/freshclam.conf_0.93: Remove the
	log file entry when logging to syslog. Otherwise we get problem
	with /etc/init.d/clamd on Gentoo.

	* modules/service_clamav/templates/clamd.conf_0.93: Remove the log
	file entry when logging to syslog. Otherwise we get problem with
	/etc/init.d/clamd on Gentoo.

	* modules/tool_portage/files/esync: Fix the update-eix call.

	* modules/tool_portage/templates/glsa-check: Improve the output
	for affected packages.

2008-08-20  Gunnar Wrobel  <p@rdus.de>

	* modules/service_postfix/templates/master.cf_2.4.6-r2: Adapt the
	kolabfilter call to hold the recipients in quotes.

2008-08-19  Gunnar Wrobel  <p@rdus.de>

	* modules/service_horde/manifests/init.pp: Allow setting horde
	admins.
	Allow for an external mail server.

	* modules/service_horde/templates/horde_kolab.php_1.1.1-r1: Allow
	setting horde admins.

2008-08-14  Gunnar Wrobel  <p@rdus.de>

	* modules/service_horde/manifests/init.pp: Hermes does not work
	based on a current release. So we'll wait here.

2008-08-13  Gunnar Wrobel  <p@rdus.de>

	* modules/service_horde/manifests/init.pp: Add horde-hermes.

	* modules/tool_system/files/rxvt-unicode: Updated to the newest
	version.

	* modules/tool_subversion/manifests/init.pp: Added subversion
	installation.

	* modules/tool_gpg/manifests/init.pp: Added gnupg installation.

	* modules/tool_emacs/manifests/init.pp: The php-mode should be
	available on all machines.

2008-08-12  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_system/manifests/init.pp: Fix the virtual decision.

	* modules/tool_portage/manifests/init.pp: Adapt to new scheme for
	adding make.conf lines.

	* modules/tool_cups/manifests/init.pp: Adapt to new scheme for
	adding make.conf lines.

	* modules/meta_de_pardus_common/manifests/init.pp: 'os' is
	required.

	* modules/service_ssmtp/*: Extracted ssmtp config from
	service_postfix.

	* modules/service_postfix/manifests/init.pp: Add the ssmtp
	configuration.

2008-08-08  Gunnar Wrobel  <p@rdus.de>

	* modules/service_horde/*: Update to the kolabified horde-webmail
	ebuild.

	* modules/service_horde/*: The base configuration for horde has
	been completed.

2008-08-07  Gunnar Wrobel  <p@rdus.de>

	* modules/service_apache/manifests/init.pp: Missing sqlite support
	in PHP.

	* modules/meta_kolab_www/manifests/init.pp: Add the horde module.

	* modules/service_horde/*: Add a draft for the horde installation.

	* modules/service_apache/files/apache2: Activate PHP.

	* modules/service_apache/manifests/init.pp: Modify the php
	installation for Horde.

	* modules/tool_baselayout/templates/hosts: Provide correct host
	settings for apache.

	* modules/meta_kolab_www/*: Add a draft of the apache definition.

	* modules/service_apache/*: Add a draft of the apache definition.

	* modules/tool_portage/manifests/init.pp: Do not add
	make.conf stuff from variable and use a virtual resource approach
	instead.

	* modules/tool_portage/templates/make.conf_2.1.4.4: Do not add
	make.conf stuff from variable and use a virtual resource approach
	instead.

	* modules/root/manifests/init.pp: Add the resource definition for
	adding lines to a file.

	* bin/pardalys: Do not fail if the network is down.

2008-08-06  Gunnar Wrobel  <p@rdus.de>

	* modules/service_spamassassin/manifests/init.pp: Fix dependencies.
	Fix the razor setup.

	* modules/service_clamav/manifests/init.pp: Fix dependencies.

	* modules/meta_kolab_monitor/manifests/init.pp: Remove the munin
	services for now.

	* modules/service_syslog/manifests/init.pp: Disable the munin
	config.

	* modules/meta_de_pardus_common/manifests/init.pp: Remove the
	munin service for now.

	* modules/tool_system/manifests/init.pp: Add net-dns/host package.

2008-07-30  Gunnar Wrobel  <p@rdus.de>

	* modules/service_openldap/templates/slapd.conf_2.4.7: Correct
	testing for the openldap_slave default value.

2008-07-28  Gunnar Wrobel  <p@rdus.de>

	* modules/service_postfix/manifests/init.pp: Add required
	dependencies for Horde_Kolab_Filter.

2008-07-27  Gunnar Wrobel  <p@rdus.de>

	* modules/service_postfix/manifests/init.pp: Add required
	dependencies for Horde_Kolab_Filter.

2008-07-26  Gunnar Wrobel  <p@rdus.de>

	* modules/service_postfix/manifests/init.pp: Do not install the
	whole Horde framework but only what we need.

	* modules/service_postfix/templates/master.cf_2.4.6-r2: Fix the
	user running the shell scripts.

	* modules/root/plugins/puppet/parser/functions/template_version.rb:
	Short comment about the format.

	* modules/service_syslog/manifests/init.pp: Corrected use of the
	package version.

	* modules/service_openldap/manifests/init.pp: Added master/slave
	replication.

	* modules/service_openldap/templates/slapd.conf_2.4.7: Added
	master/slave replication.

	* modules/tool_emacs/manifests/init.pp: Setting for a screen with
	a dark background.

	* modules/tool_emacs/templates/dot_emacs: Setting for a screen
	with a dark background.

2008-07-17  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_portage/templates/glsa-check: Display the
	vulnerable packages.

	* modules/tool_portage/files/esync: Add the eix-update call after
	syncing portage.

2008-06-30  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_cups/manifests/init.pp: Configure cups printing.

	* modules/tool_portage/templates/make.conf_2.1.4.4: Fix cflags,
	remove a few newlines.

	* modules/tool_emacs/manifests/init.pp: Add some basic
	configuration elements for emacs so that the git handling is
	always available.

	* modules/tool_emacs/templates/dot_emacs: A very basic .emacs
	file.

	* modules/tool_emacs/files/72git-gentoo.el: Always provide
	"git-status".

	* modules/meta_kolab_desktop/manifests/init.pp: Added a definition
	for a desktop machine.

	* modules/tool_system/manifests/init.pp: Add the rxvt-unicde
	terminal to the ncurses configuration.

	* modules/meta_kolab_monitor/manifests/init.pp: Added basic
	elements.

2008-06-23  Gunnar Wrobel  <p@rdus.de>

	* INSTALL: Update the install instructions.

	* modules/tool_pardalys/manifests/init.pp: Add p@rdalys
	configuration files for slave servers.

	* modules/tool_baselayout/manifests/init.pp: We need openrc for
	baselayout-2.

	* bin/dev-pardalys (GIT): Catch non-existing git.

	* modules/root/plugins/facter/package_versions.rb: Initialize the
	eix cache if it does not exist.

2008-06-03  Gunnar Wrobel  <p@rdus.de>

	* modules/service_amavisd_new/templates/amavisd.conf_2.5.2:
	Inverse logic.

	* modules/service_clamav/templates/freshclam.conf_0.93: Correct
	logging to syslog.

	* modules/service_clamav/templates/clamd.conf_0.93: Correct
	logging to syslog.

	* modules/service_amavisd_new/templates/amavisd.conf_2.5.2: Allow
	a different hostname.

	* modules/service_postfix/templates/master.cf_2.4.6-r2: Add
	support for a remote spam box.
	Allow remote reinjection.

	* modules/service_postfix/manifests/init.pp: Add support for a
	remote spam box.

	* modules/root/plugins/puppet/parser/functions/get_var.rb: Catch
	nil.

	* modules/service_kolab/plugins/facter/kolab.rb: Rewrote the
	handling of facts.

2008-06-02  Gunnar Wrobel  <p@rdus.de>

	* modules/tool_layman/plugins/puppet/type/overlay.rb: Ensure that
	our overlay list is up to date.

	* modules/service_spamassassin/templates/sa-update: Fix variable
	testing.

	* modules/service_spamassassin/manifests/init.pp: Update to the
	newer structures.

	* modules/service_kolab/plugins/facter/kolab.rb: Fix variable
	parsing.

	* modules/service_clamav/templates/freshclam.conf_0.93: Renamed
	files, fixed variables.

	* modules/service_clamav/templates/clamd.conf_0.93: Renamed files,
	fixed variable testing.

	* modules/service_clamav/manifests/init.pp: Update to the newer
	structures.

	* modules/service_amavisd_new/templates/amavisd.conf_2.5.2: Adapt
	variable names.

	* modules/service_amavisd_new/manifests/init.pp: Update to the
	newer structures.

	* modules/root/plugins/puppet/parser/functions/get_var.rb: Convert
	strings with boolean meaning to boolean values.

	* modules/meta_kolab_spam/manifests/init.pp: Adapt meta package to
	newer structures.

	* modules/meta_kolab_smtp/manifests/init.pp: Fix documentation.

	* modules/tool_emacs/manifests/init.pp: Add a few more addon
	packages.

	* modules/tool_emacs/*: Add the emacs installation.

	* modules/service_postfix/templates/kolabfilter.conf: Fix failure
	if some variables are no array.

2008-05-29  Gunnar Wrobel  <p@rdus.de>

	* modules/service_postfix/manifests/init.pp: Add the required
	kolab filter settings.

	* modules/service_postfix/templates/master.cf_2.4.6-r2: Fix the
	amavis fallback and add the kolabfilter configuration path.

	* modules/service_postfix/templates/kolabfilter.conf: Add the
	kolabfilter settings.

	* modules/service_openldap/files/kolab2.schema: Add the
	destination IMAP host.

2008-05-28  Gunnar Wrobel  <p@rdus.de>

	* modules/service_postfix/templates/kolab_smtpdpolicy.conf:
	* modules/service_postfix/templates/master.cf_2.4.6-r2:
	* modules/service_postfix/manifests/init.pp:
	Allow logging the smtpdpolicy filter and provide a configuration
	for the policy script.

	* modules/service_kolab/templates/kolab.conf: Reorder parameters.

	* modules/service_postfix/manifests/init.pp: Add perl-kolab to the
	installed packages. Add horde-framework-kolab to the installed
	packages.

	* modules/service_sasl/manifests/init.pp: Add the saslauthd
	configuration.

	* modules/service_postfix/manifests/init.pp: Add the smtpd SASL
	configuration.

	* modules/service_postfix/files/smtpd.conf: Gentoo SASL
	configuration file for the smtpd process.

	* modules/service_sasl/files/saslauthd: Gentoo configuration file
	for saslauthd.

	* modules/service_postfix/manifests/init.pp: Require the saslauthd
	service.

	* modules/meta_kolab_smtp/manifests/init.pp: Include the sasl
	module.

	* modules/meta_kolab_complete/manifests/init.pp: Include the sasl
	and postfix modules.

	* modules/service_sasl/*: Added the SASL configuration module.

	* modules/service_postfix/templates/*: Use kolab_hostname instead
	of hostname.

	* modules/service_postfix/manifests/init.pp: Finish the variable
	cleanup.

	* modules/service_kolab/templates/kolab.globals: Convert "-" to
	"_"

	* modules/service_kolab/templates/kolab.conf (php_*): Rename
	php_*. These were also counter intuitive.

	* modules/service_kolab/plugins/facter/kolab.rb: Convert
	TRUE/FALSE, convert older variable names, prepend "kolab_" to LDAP
	parameters.

2008-05-27  Gunnar Wrobel  <p@rdus.de>

	* etc/README: Add a README file.

	* modules/service_postfix/README: Add a readme for the module.

	* modules/service_postfix/templates/master.cf_2.4.6-r2: Allow
	enabling/disabling the amavis fallback.

	* modules/service_postfix/templates/main.cf_2.4.6-r2: Fix the
	filenames for the postfix configuration files.

	* modules/service_postfix/manifests/init.pp: Started cleaning
	variables and updating the docs.

	* modules/service_openldap/manifests/init.pp: Removed incorrect
	docs.

	* modules/service_kolab/templates/kolab.globals: Start documenting
	variables with relevance to postfix.

	* modules/service_postfix/manifests/init.pp: Fix a default value.

	* modules/service_kolab/plugins/facter/kolab.rb: Fix reading of
	the LDAP values into a fact.

	* modules/service_kolab/manifests/init.pp: Print the error we
	received while trying to connect the LDAP server.

	* modules/*: Added new modules.

	* facter/kolab.rb: Fix bindir and sbindir in the Kolab fact.

	* bin/pardalys: Allow a custom puppet configuration directory.

2008-05-26  Gunnar Wrobel  <p@rdus.de>

	* Makefile (SHELL): A basic Makefile for installing the package.

	* modules/tool_pardalys/manifests/init.pp: Fixed typos and revert
	back to "installed". "latest" did not seem to work at the moment.

	* modules/service_openldap/manifests/init.pp: Add the facts back
	into the manifest, add the ldap user membership in the kolab group
	and add bootstrapping.

	* modules/service_kolab/plugins/puppet/type/sslcert.rb: The self
	signed certificate type.

	* modules/service_kolab/plugins/puppet/provider/sslcert/sslcert.rb:
	A provider for a self signed certificate.

	* modules/service_openldap/plugins/puppet/type/kolabldap.rb: The
	LDAP bootstrap type.

	* modules/service_openldap/plugins/puppet/provider/kolabldap/kolabldap.rb:
	A provider for bootstrapping LDAP. This is not yet functional.

	* bin/kolab_ca: A script to create a self signed ssl certificate.

	* modules/service_kolab/plugins/facter/kolab.rb: A first draft for
	the complete kolab configuration scheme.

	* modules/service_kolab/templates/kolab.globals: Started
	converting this into a more sensible configuration file.

	* modules/service_kolab/manifests/init.pp: Add bootstrap
	functions, move facts back into the manifests, allow slapd
	recovery.

	* modules/meta_kolab_complete/manifests/init.pp: Add a clause for
	slapd recovery.

	* modules/os/manifests/init.pp: Move the facts back into the
	manifest.

	* modules/service_kolab/plugins/facter/*: Renamed facts more
	appropriately.

2008-05-23  Gunnar Wrobel  <p@rdus.de>

	* modules/root/spec/spec_helper.rb: A unit test helper.

	* modules/root/plugins/puppet/parser/functions/template_version.rb:
	Tried fixing some problmens after unit testing.

	* modules/root/spec/unit/parser/functions/template_version.rb: A
	first unit test for plugin functions.

	* modules/tool_pardalys/manifests/init.pp: Require the latest
	pardalys.

	* modules/meta_kolab_complete/manifests/init.pp: Include
	tool::pardalys.

	* modules/service_kolab/manifests/init.pp: Added facts.

	* modules/service_kolab/plugins/facter/kolab.rb: Add additional
	facts.

	* modules/service_kolab/templates/kolab.conf (postfix_mydomain):
	Added postfix_mydomain value.
	(bind_dn): Replace some variables by facts.

	* modules/service_kolab/manifests/init.pp: Moved the default value
	handling into the fact script.

	* modules/service_kolab/plugins/puppet/*: Moved the ruby functions
	into the fact script.

	* modules/service_kolab/plugins/facter/kolab.rb: Integrated
	default value handling from the manifast into the fact script.

	* modules/tool_pardalys/*: Added package installation module for
	p@rdalys.

2008-05-21  Gunnar Wrobel  <p@rdus.de>

	* modules/service_openldap/manifests/init.pp: Allow writing the
	first openldap configuration files.

	* modules/service_openldap/templates/slapd.conf_2.4.7: Fix
	variable names.

	* modules/service_openldap/templates/slapd.access_2.4.7: Fix
	variable names.

	* modules/service_openldap/plugins/facter/01_openldap_paths.rb:
	Fix setting the openldap facts.

	* modules/root/plugins/facter/package_versions.rb: Fix this fact
	so that it can be used in a sensible way.

	* etc/package_versions: Add a supporting file for the package
	version fact.

	* modules/service_kolab/templates/kolab.conf: Remove newlines.

	* modules/service_kolab/plugins/facter/kolab.rb: Fix the ruby code
	for this fact.

	* modules/service_kolab/manifests/init.pp: Do not recalculate
	kolab_bind_pw_hash each time.

2008-05-20  Gunnar Wrobel  <p@rdus.de>

	* modules/service_kolab/manifests/init.pp: Fixed typos, variable
	names and added the kolab config directory.

	* modules/service_kolab/plugins/facter/kolab.rb: Fixed variable
	names.

	* modules/service_kolab/templates/kolab.conf: Fixed variable
	names.

	* modules/service_kolab/templates/kolab.globals: Removed some
	variables we don't need at the moment and fixed variable names.

	* modules/service_kolab/plugins/puppet/parser/functions/shellquote.rb:
	Added custom function.

	* modules/service_kolab/plugins/puppet/parser/functions/dnfromdomain.rb:
	Added custom function.

	* modules/os/plugins/facter/00_paths.rb: Fixed typos.

	* modules/service_kolab/plugins/facter/01_kolab_paths.rb: Renamed
	from 01_path.rb.

	* etc/puppet.conf: Remove the modulepath here.

	* bin/pardalys: Add the modulepath

	* modules/meta_kolab_complete/manifests/init.pp: Import the root
	module to make its functions available.

	* modules/service_openldap/*: A first draft for the configuration
	of the OpenLDAP slapd server.

	* modules/service_kolab/*: Added new module that provides the
	basic kolab files and should later hold the bootstrapping process.

	* modules/root/*: Added new module that will hold basic extensions.

	* modules/os_gentoo/plugins/facter/keyword.rb: Include a check for
	the operating system.

	* modules/os/plugins/facter/00_paths.rb: Added facts for the
	system settings.

	* modules/os_gentoo/manifests/init.pp: Removed settings class.

	* modules/os/manifests/init.pp: The basic system settings are now
	handled as facts

	* modules/meta_kolab_complete/manifests/init.pp: Included two new
	classes.

2008-05-18  Gunnar Wrobel  <p@rdus.de>

	* modules/os_gentoo/manifests/init.pp: Simplify the file creation
	in /etc/portage/package.*/ and rely on the defaults.

2008-05-17  Gunnar Wrobel  <p@rdus.de>

	* etc/puppet.conf: Remove the factpath here.

	* bin/pardalys (FACTS): Set the correct factpath.

	* bin/dev-pardalys: Allow specifying the connection method.
	Fixed separator for ssh connections.

	* modules/service_openldap/manifests/init.pp: Fixes to the
	openldap installation section.

	* modules/os_gentoo/plugins/facter/keyword.rb: Determines the
	system keyword.

	* modules/os/manifests/init.pp: Allow to backup the
	/etc/portage/package.* files.

	* etc/puppet.conf: Specify the factpath.

	* bin/pardalys: Allow additional parameters for the puppet call

	* etc/puppet.conf: Configure two different environments.

	* bin/pardalys (ENVIRONMENT): Add ENVIRONMENT setting.

	* etc/puppet.conf: Simplify the initial puppet configuration.

	* bin/pardalys: Allow for a development environment.

	* bin/dev-pardalys: Added a simple pardalys checkout script for
	the hackers among the Kolab2/Gentoo users.

	* INSTALL: Fixed required puppet version though that one is not
	released yet.

	* manifests/kolab.pp: The basic node definition that will pull in
	a complete Kolab server.

	* modules/*: Added a basic set of modules.

2008-05-14  Gunnar Wrobel  <p@rdus.de>

	* etc/puppet.conf: Added a first dummy puppet.conf

	* bin/pardalys: Added a simple shell script.

	* AUTHORS: Added.

	* CHANGES: Added.

	* COPYING: Added.

	* HACKING: Added.

	* RELEASE_NOTES: Added.

	* INSTALL: Added.

	* README: Added.

	* TODO: Added.
