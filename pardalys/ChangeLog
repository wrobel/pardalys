2008-05-28  Gunnar Wrobel  <p@rdus.de>

	* modules/service_postfix/templates/*: Use kolab_hostname instead
	of hostname.

	* modules/service_postfix/manifests/init.pp: Finish the variable
	cleanup.

	* modules/service_kolab/templates/kolab.globals: Convert "-" to
	"_"

	* modules/service_kolab/templates/kolab.conf (php_*): Rename
	php_*. These were also counter intuitive.

	* modules/service_kolab/plugins/facter/kolab.rb: Convert
	TRUE/FALSE, convert older variable names, prepend "kolab_" to LDAP
	parameters.

2008-05-27  Gunnar Wrobel  <p@rdus.de>

	* modules/service_postfix/README: Add a readme for the module.

	* modules/service_postfix/templates/master.cf_2.4.6-r2: Allow
	enabling/disabling the amavis fallback.

	* modules/service_postfix/templates/main.cf_2.4.6-r2: Fix the
	filenames for the postfix configuration files.

	* modules/service_postfix/manifests/init.pp: Started cleaning
	variables and updating the docs.

	* modules/service_openldap/manifests/init.pp: Removed incorrect
	docs.

	* modules/service_kolab/templates/kolab.globals: Start documenting
	variables with relevance to postfix.

	* modules/service_postfix/manifests/init.pp: Fix a default value.

	* modules/service_kolab/plugins/facter/kolab.rb: Fix reading of
	the LDAP values into a fact.

	* modules/service_kolab/manifests/init.pp: Print the error we
	received while trying to connect the LDAP server.

	* modules/*: Added new modules.

	* facter/kolab.rb: Fix bindir and sbindir in the Kolab fact.

	* bin/pardalys: Allow a custom puppet configuration directory.

2008-05-26  Gunnar Wrobel  <p@rdus.de>

	* Makefile (SHELL): A basic Makefile for installing the package.

	* modules/tool_pardalys/manifests/init.pp: Fixed typos and revert
	back to "installed". "latest" did not seem to work at the moment.

	* modules/service_openldap/manifests/init.pp: Add the facts back
	into the manifest, add the ldap user membership in the kolab group
	and add bootstrapping.

	* modules/service_kolab/plugins/puppet/type/sslcert.rb: The self
	signed certificate type.

	* modules/service_kolab/plugins/puppet/provider/sslcert/sslcert.rb:
	A provider for a self signed certificate.

	* modules/service_openldap/plugins/puppet/type/kolabldap.rb: The
	LDAP bootstrap type.

	* modules/service_openldap/plugins/puppet/provider/kolabldap/kolabldap.rb:
	A provider for bootstrapping LDAP. This is not yet functional.

	* bin/kolab_ca: A script to create a self signed ssl certificate.

	* modules/service_kolab/plugins/facter/kolab.rb: A first draft for
	the complete kolab configuration scheme.

	* modules/service_kolab/templates/kolab.globals: Started
	converting this into a more sensible configuration file.

	* modules/service_kolab/manifests/init.pp: Add bootstrap
	functions, move facts back into the manifests, allow slapd
	recovery.

	* modules/meta_kolab_complete/manifests/init.pp: Add a clause for
	slapd recovery.

	* modules/os/manifests/init.pp: Move the facts back into the
	manifest.

	* modules/service_kolab/plugins/facter/*: Renamed facts more
	appropriately.

2008-05-23  Gunnar Wrobel  <p@rdus.de>

	* modules/root/spec/spec_helper.rb: A unit test helper.

	* modules/root/plugins/puppet/parser/functions/template_version.rb: 
	Tried fixing some problmens after unit testing.

	* modules/root/spec/unit/parser/functions/template_version.rb: A
	first unit test for plugin functions.

	* modules/tool_pardalys/manifests/init.pp: Require the latest
	pardalys.

	* modules/meta_kolab_complete/manifests/init.pp: Include
	tool::pardalys.

	* modules/service_kolab/manifests/init.pp: Added facts.

	* modules/service_kolab/plugins/facter/kolab.rb: Add additional
	facts.

	* modules/service_kolab/templates/kolab.conf (postfix_mydomain):
	Added postfix_mydomain value.
	(bind_dn): Replace some variables by facts.

	* modules/service_kolab/manifests/init.pp: Moved the default value
	handling into the fact script.

	* modules/service_kolab/plugins/puppet/*: Moved the ruby functions
	into the fact script.

	* modules/service_kolab/plugins/facter/kolab.rb: Integrated
	default value handling from the manifast into the fact script.
	
	* modules/tool_pardalys/*: Added package installation module for
	p@rdalys.

2008-05-21  Gunnar Wrobel  <p@rdus.de>

	* modules/service_openldap/manifests/init.pp: Allow writing the
	first openldap configuration files.

	* modules/service_openldap/templates/slapd.conf_2.4.7: Fix
	variable names.

	* modules/service_openldap/templates/slapd.access_2.4.7: Fix
	variable names.

	* modules/service_openldap/plugins/facter/01_openldap_paths.rb:
	Fix setting the openldap facts.

	* modules/root/plugins/facter/package_versions.rb: Fix this fact
	so that it can be used in a sensible way.

	* etc/package_versions: Add a supporting file for the package
	version fact.

	* modules/service_kolab/templates/kolab.conf: Remove newlines.

	* modules/service_kolab/plugins/facter/kolab.rb: Fix the ruby code
	for this fact.

	* modules/service_kolab/manifests/init.pp: Do not recalculate
	kolab_bind_pw_hash each time.

2008-05-20  Gunnar Wrobel  <p@rdus.de>

	* modules/service_kolab/manifests/init.pp: Fixed typos, variable
	names and added the kolab config directory.

	* modules/service_kolab/plugins/facter/kolab.rb: Fixed variable
	names.

	* modules/service_kolab/templates/kolab.conf: Fixed variable
	names.

	* modules/service_kolab/templates/kolab.globals: Removed some
	variables we don't need at the moment and fixed variable names.

	* modules/service_kolab/plugins/puppet/parser/functions/shellquote.rb:
	Added custom function.

	* modules/service_kolab/plugins/puppet/parser/functions/dnfromdomain.rb:
	Added custom function.

	* modules/os/plugins/facter/00_paths.rb: Fixed typos.

	* modules/service_kolab/plugins/facter/01_kolab_paths.rb: Renamed
	from 01_path.rb.

	* etc/puppet.conf: Remove the modulepath here.

	* bin/pardalys: Add the modulepath

	* modules/meta_kolab_complete/manifests/init.pp: Import the root
	module to make its functions available.

	* modules/service_openldap/*: A first draft for the configuration
	of the OpenLDAP slapd server.

	* modules/service_kolab/*: Added new module that provides the
	basic kolab files and should later hold the bootstrapping process.

	* modules/root/*: Added new module that will hold basic extensions.

	* modules/os_gentoo/plugins/facter/keyword.rb: Include a check for
	the operating system.

	* modules/os/plugins/facter/00_paths.rb: Added facts for the
	system settings.

	* modules/os_gentoo/manifests/init.pp: Removed settings class.

	* modules/os/manifests/init.pp: The basic system settings are now
	handled as facts

	* modules/meta_kolab_complete/manifests/init.pp: Included two new
	classes.

2008-05-18  Gunnar Wrobel  <p@rdus.de>

	* modules/os_gentoo/manifests/init.pp: Simplify the file creation
	in /etc/portage/package.*/ and rely on the defaults.

2008-05-17  Gunnar Wrobel  <p@rdus.de>

	* etc/puppet.conf: Remove the factpath here.

	* bin/pardalys (FACTS): Set the correct factpath.

	* bin/dev-pardalys: Allow specifying the connection method.
	Fixed separator for ssh connections.

	* modules/service_openldap/manifests/init.pp: Fixes to the
	openldap installation section.

	* modules/os_gentoo/plugins/facter/keyword.rb: Determines the
	system keyword.

	* modules/os/manifests/init.pp: Allow to backup the
	/etc/portage/package.* files.

	* etc/puppet.conf: Specify the factpath.

	* bin/pardalys: Allow additional parameters for the puppet call

	* etc/puppet.conf: Configure two different environments.

	* bin/pardalys (ENVIRONMENT): Add ENVIRONMENT setting.

	* etc/puppet.conf: Simplify the initial puppet configuration.

	* bin/pardalys: Allow for a development environment.

	* bin/dev-pardalys: Added a simple pardalys checkout script for
	the hackers among the Kolab2/Gentoo users.

	* INSTALL: Fixed required puppet version though that one is not
	released yet.

	* manifests/kolab.pp: The basic node definition that will pull in
	a complete Kolab server.

	* modules/*: Added a basic set of modules.

2008-05-14  Gunnar Wrobel  <p@rdus.de>

	* etc/puppet.conf: Added a first dummy puppet.conf

	* bin/pardalys: Added a simple shell script.

	* AUTHORS: Added.

	* CHANGES: Added.

	* COPYING: Added.

	* HACKING: Added.

	* RELEASE_NOTES: Added.

	* INSTALL: Added.

	* README: Added.

	* TODO: Added.

